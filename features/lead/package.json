{
  "name": "@interaction-sdk/feature-lead",
  "version": "0.1.0",
  "type": "module",
  "sideEffects": false,
  "files": ["dist"],
  "exports": { ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" } },
  "dependencies": {
    "@interaction-sdk/contracts": "workspace:*",
    "@interaction-sdk/core": "workspace:*",
    "@interaction-sdk/state": "workspace:*",
    "zod": "^4.0.0",
    "zustand": "^5.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  }
}
